<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardView"
        android:layout_width="match_parent"
        android:layout_height="70dp"
      android:padding="3dp"

       >

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/bg">

            <ImageView
                android:id="@+id/img_folder"
                android:layout_width="80dp"
                android:layout_height="match_parent"
                android:contentDescription="@string/folder"
                app:srcCompat="@drawable/ic_baseline_folder_24" />

            <TextView
                android:id="@+id/txt_videoTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toEndOf="@+id/img_folder"
                android:textColor="@color/txcolor"
                android:singleLine="true"
                android:textSize="20sp" />

            <TextView
                android:id="@+id/txt_videoCount"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_below="@id/txt_videoTitle"
                android:layout_toEndOf="@id/img_folder"
                android:textColor="@color/vdcolo"
                android:textSize="15sp" />

        </RelativeLayout>

        <TableRow
            android:id="@+id/hr"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#444" />

    </androidx.cardview.widget.CardView>
</RelativeLayout>